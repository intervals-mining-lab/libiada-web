@{
    ViewBag.Title = "Libiada - Custom sequences segmentation results";
}

<h2>@ViewBag.Title</h2>
@if (ViewBag.Error == null)
{
    await Html.RenderPartialAsync("_AngularControllerInitializer", "CustomSequenceSegmentationResult");

    <div ng-controller="CustomSequenceSegmentationResultCtrl">
        <loading-window loading="loading" loading-screen-header="loadingScreenHeader"></loading-window>
        <div ng-disabled="loading">
            <table class="table table-bordered table-hover table-sm" ng-cloak>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Sequence</th>
                    </tr>
                </thead>
                <tbody>
                    <tr ng-repeat="sequence in sequences">
                        <td ng-bind="sequence.name" />
                        <td ng-bind="sequence.value" />
                    </tr>
                </tbody>
            </table>
        </div>
    </div>

    @section Scripts {
    }
}
