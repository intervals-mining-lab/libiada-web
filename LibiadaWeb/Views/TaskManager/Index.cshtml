@{
    ViewBag.Title = "Менеджер задач";
}

<h2>Index</h2>

@if (ViewBag.ErrorMessage != null)
{
    <div class="error-message">
        @ViewBag.ErrorMessage
    </div>
    <br/>
}

<table class="table">
    <tr>
        <th>
            Task id
        </th>
        <th>
            Task Name
        </th>
        <th>
            Created
        </th>
        <th>
            Task state
        </th>
        <th>
            Actions
        </th>
    </tr>

    @foreach (var item in ViewBag.Tasks)
    {
        <tr>
            <td>
                @item.Key
            </td>
            <td>
                @item.Value.ActionName
            </td>
            <td>
                @item.Value.Created
            </td>
            <td>
                @item.Value.TaskState
            </td>
            <td>
                @Html.ActionLink("View result", "RedirectToResult", new { id = item.Key })
            </td>
        </tr>
    }
</table>