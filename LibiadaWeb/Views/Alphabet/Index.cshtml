@model IEnumerable<LibiadaWeb.alphabet>

@{
    ViewBag.Title = "Алфавиты";
}

<h2>Перечень алфавитов</h2>

<p>
    @Html.ActionLink("Создать", "Create")
</p>
<table>
    <tr>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Редактировать", "Edit", new { /*id=item.PrimaryKey */ }) |
            @Html.ActionLink("Подробности", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Удалить", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
