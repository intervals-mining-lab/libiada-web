@using LibiadaWeb.Models
@model IEnumerable<Matter>

@{
    ViewBag.Title = "Clusterization";
}

<script type="text/javascript">
    "use strict";

    var data = @Html.Raw(Json.Encode(ViewBag.data));
</script>

<h2>Clusterization</h2>

@using (Html.BeginForm("Index", "Clusterization", FormMethod.Post, new { enctype = "multipart/form-data", ng_app = "Calculation" }))
{
    <div class="form-horizontal" ng-controller="CalculationCtrl">
        <h4>Sequences and parameters</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <br />
        <div class="form-group">
            @Html.LabelFor(model => model.First().NatureId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select class="form-control" ng-model="natureId" ng-options="n.Value as n.Text for n in natures"></select>
                @Html.ValidationMessage("natureId", "", new { @class = "text-danger" })
            </div>
        </div>
        @Html.Label("matterIds", "Sequences", new { @class = "control-label" })
        <table class="table table-bordered table-hover">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.Name)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Description)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Created)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.Modified)
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr ng-repeat="matter in matters | filter:{Nature: natureId}">
                    <td>
                        <label>
                            <input type="checkbox" name="matterIds" ng-value="matter.Value" />
                            {{matter.Text}}
                        </label>
                    </td>
                    <td ng-bind="matter.Description" />
                    <td ng-bind="matter.Created" />
                    <td ng-bind="matter.Modified" />
                </tr>
            </tbody>
        </table>
        @Html.Partial("_Characteristics")
        <div class="form-group">
            @Html.Label("clustersCount", "Clusters count", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input class="form-control" name="clustersCount" type="number" value="2" min="2" />
                @Html.ValidationMessage("clustersCount", "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            @Html.Label("equipotencyWeight", "Equipotency weight", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input class="form-control" name="equipotencyWeight" type="number" value="4" min="0" />
                @Html.ValidationMessage("equipotencyWeight", "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            @Html.Label("normalizedDistanceWeight", "Normalized distance weight", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input class="form-control" name="normalizedDistanceWeight" type="number" value="2" min="0" />
                @Html.ValidationMessage("normalizedDistanceWeight", "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            @Html.Label("distanceWeight", "Normalized distance weight", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input class="form-control" name="distanceWeight" type="number" value="1" min="0"/>
                @Html.ValidationMessage("distanceWeight", "", new { @class = "text-danger" })
            </div>
        </div>
        <br />
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Calculate" class="btn btn-default" />
            </div>
        </div>
    </div>
}

@section Scripts {
    @Scripts.Render("~/bundles/angularjs")
    @Scripts.Render("~/bundles/procedures")
    @Scripts.Render("~/bundles/controllers/calculation")
}
