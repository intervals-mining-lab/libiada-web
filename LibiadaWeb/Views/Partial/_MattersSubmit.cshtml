<div class="form-group">
    <div class="col-md-10">
        <input type="submit" value="{{submitName}}" class="btn btn-default" ng-disabled=" (matters | filter:{ Selected: true }).length < minimumSelectedMatters" />
    </div>
</div>