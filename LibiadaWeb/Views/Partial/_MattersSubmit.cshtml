@model dynamic

<div class="form-group">
    <div class="col-md-10">
        <input type="submit" value="{{submitName}}" class="btn btn-default" ng-disabled="selectedMatters < minimumSelectedMatters" />
    </div>
</div>