@using LibiadaWeb.Models
@model Matter

@{
    ViewBag.Title = "Create matter";
}

<script>
    "use strict";

    var data = @Html.Raw(Json.Encode(ViewBag.data));
</script>

<h2>Create matter</h2>


@using (Html.BeginForm("Create", "Matter", FormMethod.Post, new { enctype = "multipart/form-data", ng_app = "MattersequenceCreate" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal" ng-controller="MattersequenceCreateCtrl">
        <h4>Matter</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Name)
                @Html.ValidationMessageFor(model => model.Name)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Description, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Description)
                @Html.ValidationMessageFor(model => model.Description)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Загружать цепочку из локального файла", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.CheckBox("localFile", new { ng_model = "localFile" })
            </div>
        </div>

        <div class="form-group" ng-show="localFile">
            @Html.Label("Файл цепочки", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input type="file" name="file" id="file" />
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.NatureId, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select ng-model="natureId" ng-options="n.Value as n.Text for n in natures"></select>
                <input type="hidden" name="NatureId" ng-value="natureId" />
                @Html.ValidationMessageFor(model => model.NatureId)
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Форма записи цепочки", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select ng-model="notationId" ng-options="n.Value as n.Text for n in notationsFiltered"></select>
                <input type="hidden" name="notationId" ng-value="notationId" />
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Тип цепочки", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select ng-model="pieceTypeId" ng-options="p.Value as p.Text for p in pieceTypesFiltered"></select>
                <input type="hidden" name="pieceTypeId" ng-value="pieceTypeId" />
            </div>
        </div>

        <div class="form-group">
            @Html.Label("Стороняя БД", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select ng-model="remoteDbId" ng-options="r.Value as r.Text for r in remoteDbsFiltered"></select>
                <input type="hidden" name="remoteDbId" ng-value="remoteDbId" />
            </div>
        </div>

        <div class="form-group" ng-show="isRemoteDbDefined()">
            @Html.Label("id в сторонней БД", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <input type="text" name="remoteId" ng-model="remoteId" />
            </div>
        </div>

        <div ng-switch="natureId">
            <div ng-switch-when="@Aliases.Nature.Genetic">
                <div class="form-group">
                    @Html.Label("Продукт последовательности", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        <select ng-model="productId" ng-options="l.Value as l.Text for l in products"></select>
                        <input type="hidden" name="productId" ng-value="productId" />
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("Неполная последовательность", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.CheckBox("partial")
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("Комплементарная цепочка", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.CheckBox("complement")
                    </div>
                </div>
            </div>

            <div ng-switch-when="@Aliases.Nature.Literature">
                <div class="form-group">
                    @Html.Label("Язык литературного произведения", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        <select ng-model="languageId" ng-options="l.Value as l.Text for l in languages"></select>
                        <input type="hidden" name="languageId" ng-value="languageId" />
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("Произведение является оригиналом, а не переводом", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        @Html.CheckBox("original")
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("Переводчик", new { @class = "control-label col-md-2" })
                    <div class="col-md-10">
                        <select ng-model="translatorId" ng-options="t.Value as t.Text for t in translators"></select>
                        <input type="hidden" name="translatorId" ng-value="translatorId" />
                    </div>
                </div>
            </div>

            <div ng-switch-when="@Aliases.Nature.Music">

            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Создать" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/angularjs")
    @Scripts.Render("~/bundles/procedures")
    @Scripts.Render("~/bundles/controllers/matterSequenceCreate")
}
